# ============================================================
# FixTrade — Python Dependencies
# Each dependency is justified. No wildcard installs.
# ============================================================

# --- Web Framework ---
# FastAPI: ASGI web framework for building the REST API
fastapi==0.115.0
# Uvicorn: ASGI server to run FastAPI in development and production
uvicorn[standard]==0.30.6
# Pydantic-settings: settings management via .env files (Pydantic v2)
pydantic-settings==2.5.2

# --- Security ---
# slowapi: Rate limiting middleware for FastAPI/Starlette
slowapi==0.1.9

# --- WSGI Compatibility ---
# asgiref: ASGI-to-WSGI bridge for legacy server deployments
asgiref==3.8.1
# gunicorn: Production WSGI/ASGI server
gunicorn==23.0.0

# --- Scraping ---
# Scrapy: Web scraping framework for news article collection
scrapy==2.11.2

# --- Database ---
# SQLAlchemy: ORM and DB toolkit
SQLAlchemy==2.0.46
# pg8000: Pure-Python Postgres driver (no native build required on Windows)
pg8000==1.29.7
# python-dateutil: Robust date parsing (fallback)
python-dateutil==2.8.2
# dateparser: Flexible, multi-locale date parsing for scraped dates
dateparser==1.1.8
# dateparser: parse dates in multiple locales (English/French, etc.)
dateparser==1.1.8

# --- NLP ---
# transformers: HuggingFace transformer models for sentiment analysis
transformers==4.51.3

# --- Data Processing ---
# pandas: DataFrame operations for ETL and feature engineering
pandas>=2.1.0
# numpy: Numerical computing for ML pipelines
numpy>=1.26.0
# pyarrow: Parquet file read/write (columnar storage ~80% compression)
pyarrow>=14.0.0

# --- Machine Learning ---
# scikit-learn: MinMaxScaler, metrics, preprocessing utilities
scikit-learn>=1.4.0
# xgboost: Gradient-boosted decision tree model
xgboost>=2.0.0
# prophet: Facebook Prophet for trend + seasonality decomposition
prophet>=1.1.5
# torch: PyTorch for LSTM neural network model
torch>=2.1.0

# --- Caching & Feature Store ---
# redis: Redis client for prediction cache and feature store
redis>=5.0.0

# --- Experiment Tracking ---
# mlflow: Model registry, experiment tracking, metric comparison
mlflow>=2.10.0

# --- Testing ---
# pytest: Test runner
pytest==8.3.3
# httpx: Async HTTP client, required by FastAPI TestClient
httpx==0.27.2
